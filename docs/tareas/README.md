# docs/tareas

Una carpeta por tarea (`<task_id>/`) con sus artefactos del ciclo F5: `design.md`, `manual-prueba.md` e imágenes (`img/`).
Todo se versiona en git: es el histórico auditable de lo que la IA diseñó y probó por tarea.
